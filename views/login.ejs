<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>DeadLine Drift - Admin Login</title>
  <style>
    body { background: #0f0f0f; color: #fff; font-family: 'Segoe UI', sans-serif; text-align: center; padding: 50px; }
    .container { max-width: 400px; margin: 0 auto; background: #1a1a1a; padding: 40px; border-radius: 12px; box-shadow: 0 0 20px rgba(0,255,100,0.2); }
    input { width: 100%; padding: 12px; margin: 10px 0; background: #222; border: 1px solid #0f0; color: #fff; border-radius: 6px; }
    button { background: #0f0; color: #000; padding: 12px 30px; border: none; border-radius: 6px; font-weight: bold; cursor: pointer; }
    .error { color: #ff4444; }
  </style>
</head>
<body>
  <div class="container">
    <h1>🔐 DeadLine Drift</h1>
    <h2>Admin Login</h2>
    <% if (error) { %><p class="error"><%= error %></p><% } %>
    <form method="POST" action="/login">
      <input type="email" name="email" placeholder="Email" required>
      <input type="password" name="password" placeholder="Password" required>
      <button type="submit">Login</button>
    </form>
    <p style="margin-top:20px; font-size:0.9em;">Default: admin@deadlinedrift.com / admin123</p>
  </div>
</body>
</html>